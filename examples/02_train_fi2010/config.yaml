data:
  window: 100
  horizon_index: 4
  num_features: 40
  batch_size: 64
  num_workers: 0
  shuffle_train: true
model:
  conv_channels: 32
  fusion_channels: 64
  lstm_hidden: 64
  lstm_layers: 1
  num_classes: 3
  leaky_slope: 0.01
  recurrent_dropout: 0.0
  num_features: 40
train:
  epochs: 50
  learning_rate: 0.0001
  weight_decay: 0.0
  grad_clip: null
  early_stop_patience: 10
  seed: 42
  device: auto
  checkpoint_dir: checkpoints
  log_level: INFO
